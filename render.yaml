services:
  - type: web
    name: app-setup-portal
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: APP_PASSWORD
        value: admin123
      - key: SESSION_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
